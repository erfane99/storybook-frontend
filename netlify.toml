[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# Using Next.js runtime instead of manual redirects
[[plugins]]
  package = "@netlify/plugin-nextjs"